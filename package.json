{
  "name": "leave-tracker",
  "version": "2.0.0",
  "description": "Simple team leave tracker — invite links, department-head approvals, public holidays, HR reports. Deploys to Netlify (Netlify Blobs storage) or runs as a standalone Node server.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.0"
  }
}
